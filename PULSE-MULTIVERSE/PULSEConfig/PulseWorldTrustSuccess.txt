<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport"
      content="width=device-width,
               initial-scale=1,
               maximum-scale=1,
               minimum-scale=1,
               user-scalable=no">

<title>PulseWorld OS — Transmission Confirmed</title>


<style>
/* ============================================================
   ⭐ GLOBAL
============================================================ */
html, body {
  margin: 0;
  padding: 0;
  background: #000;
  color: #0ff;
  font-family: system-ui, sans-serif;
  overflow: hidden;
}

/* ============================================================
   ⭐ PARALLAX BACKGROUND
============================================================ */
.bg {
  position: fixed;
  inset: 0;
  background-size: cover;
  background-position: center;
  opacity: 0.35;
  pointer-events: none;
  animation: drift 45s ease-in-out infinite;
  z-index: 1;
}

#bg1 { background-image: url("./_EXPRESSIONS/_PEX/BUILD/PulseWorldBarrier-Alpha.png"); opacity: 0.45; animation-duration: 55s; }
#bg2 { background-image: url("./_EXPRESSIONS/_PEX/BUILD/PulseNebulaLayer1.png"); opacity: 0.25; animation-duration: 75s; }
#bg3 { background-image: url("./_EXPRESSIONS/_PEX/BUILD/PulseNebulaLayer2.png"); opacity: 0.18; animation-duration: 95s; }

@keyframes drift {
  0%   { transform: scale(1) translate(0,0); }
  50%  { transform: scale(1.05) translate(2%, -1%); }
  100% { transform: scale(1) translate(0,0); }
}

/* ============================================================
   ⭐ SCANLINES + ABERRATION
============================================================ */
#scanlines {
  position: fixed;
  inset: 0;
  background: repeating-linear-gradient(
    to bottom,
    rgba(0,255,255,0.05) 0px,
    rgba(0,255,255,0.05) 1px,
    transparent 2px,
    transparent 4px
  );
  mix-blend-mode: overlay;
  opacity: 0.25;
  pointer-events: none;
  z-index: 9997;
}

#aberration {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 9996;
  background: radial-gradient(circle at center,
    rgba(255,0,0,0.08),
    rgba(0,255,255,0.08),
    transparent 70%
  );
  animation: aberrationPulse 3.5s infinite ease-in-out;
}

@keyframes aberrationPulse {
  0% { opacity: 0.05; }
  50% { opacity: 0.15; }
  100% { opacity: 0.05; }
}

/* ============================================================
   ⭐ SUCCESS PANEL
============================================================ */
#panel {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 60vw;
  max-width: 650px;
  transform: translate(-50%, -50%);
  padding: 40px;
  background: rgba(0,0,0,0.55);
  border: 1px solid rgba(0,255,255,0.35);
  border-radius: 20px;
  backdrop-filter: blur(18px);
  box-shadow: 0 0 40px rgba(0,255,255,0.35);
  text-align: center;
  z-index: 9999;
  animation: panelFade 1.2s ease-out forwards;
  opacity: 0;
}

@keyframes panelFade {
  from { opacity: 0; transform: translate(-50%, -48%); }
  to   { opacity: 1; transform: translate(-50%, -50%); }
}

/* ============================================================
   ⭐ SUCCESS GLYPH
============================================================ */
#successGlyph {
  width: 140px;
  height: 140px;
  margin: 0 auto 20px;
  border-radius: 50%;
  border: 3px solid #00ffa6;
  position: relative;
  animation: pulseGlow 2.2s infinite ease-in-out;
  box-shadow: 0 0 25px rgba(0,255,255,0.45);
}

#successGlyph::after {
  content: "";
  position: absolute;
  left: 42px;
  top: 28px;
  width: 40px;
  height: 70px;
  border-right: 6px solid #00ffa6;
  border-bottom: 6px solid #00ffa6;
  transform: rotate(45deg);
}

@keyframes pulseGlow {
  0% { transform: scale(1); box-shadow: 0 0 20px rgba(0,255,255,0.35); }
  50% { transform: scale(1.08); box-shadow: 0 0 35px rgba(0,255,255,0.55); }
  100% { transform: scale(1); box-shadow: 0 0 20px rgba(0,255,255,0.35); }
}

/* ============================================================
   ⭐ TEXT
============================================================ */
#panel h1 {
  font-size: 2.8vw;
  margin-bottom: 10px;
  text-shadow: 0 0 12px rgba(0,255,255,0.75);
}

#panel p {
  font-size: 1.2vw;
  opacity: 0.85;
  margin-bottom: 20px;
}

/* ============================================================
   ⭐ EMAIL PILL
============================================================ */
.email-pill {
  display: inline-block;
  padding: 8px 18px;
  border-radius: 999px;
  background: rgba(0,255,255,0.12);
  border: 1px solid rgba(0,255,255,0.35);
  color: #00ffa6;
  font-size: 1vw;
  font-weight: 600;
  margin-bottom: 20px;
  backdrop-filter: blur(6px);
}

/* ============================================================
   ⭐ BUTTON
============================================================ */
.btn {
  display: inline-block;
  padding: 14px 28px;
  border-radius: 14px;
  border: 1px solid #0ff;
  background: rgba(0,0,0,0.85);
  color: #0ff;
  font-size: 1.2vw;
  text-decoration: none;
  cursor: pointer;
  box-shadow: 0 0 18px rgba(0,255,255,0.35);
  transition: 0.15s ease-out;
  margin-top: 10px;
}

.btn:hover {
  transform: translateY(-3px);
  box-shadow: 0 0 28px rgba(0,255,255,0.55);
}
</style>
</head>

<body>

<!-- ⭐ BACKGROUND -->
<div id="bg1" class="bg"></div>
<div id="bg2" class="bg"></div>
<div id="bg3" class="bg"></div>

<!-- ⭐ SCANLINES + ABERRATION -->
<div id="scanlines"></div>
<div id="aberration"></div>

<!-- ⭐ SUCCESS PANEL -->
<div id="panel">

  <div id="successGlyph"></div>

  <h1>Transmission Confirmed</h1>
  <p>
    Your secure PulseWorld OS transmission has been successfully dispatched.  
    A confirmation has been routed to your inbox.
  </p>

  <div class="email-pill">
    Sent to: <span id="email-target">{displayName}</span>
  </div>

  <a id="returnBtn" class="btn" href="#">Return to PulseWorld OS</a>
</div>

<script type="module">
document.addEventListener("DOMContentLoaded", () => {

  const params = new URLSearchParams(window.location.search);

  // ⭐ NEW: Pull displayName from param
  const displayName =
    params.get("user") ||
    params.get("displayName") ||
    "Friend";

  // ⭐ Update UI
  document.getElementById("email-target").textContent = displayName;

  // ⭐ Keep passing displayName forward
  const returnURL =
    "/?pulseuser=" +  encodeURIComponent(displayName);

  document.getElementById("returnBtn").href = returnURL;

});
</script>

</body>
</html>
