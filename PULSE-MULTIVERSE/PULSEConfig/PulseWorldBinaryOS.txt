<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">

<title>BinaryOS.net — Coming Soon</title>

<!-- ============================================================
     PREFETCH / PRELOAD
     ============================================================ -->
<link rel="prefetch" fetchpriority="high" href="/">
<link rel="preload" fetchpriority="high" as="image" href="./_EXPRESSIONS/_PEX/BUILD/BinaryOSNet.webp.pex" type="image/webp"/>
<link rel="preload" fetchpriority="high" as="image" href="./_EXPRESSIONS/_PEX/BUILD/BinaryOS.webp.pex" type="image/webp"/>

<style>
  /* ============================================================
     GLOBAL PAGE STYLE
     ============================================================ */


  /* Fullscreen background */
  .bg {
    position: fixed;
    inset: 0;
    background: url('./_EXPRESSIONS/_PEX/BUILD/BinaryOSNet.webp.pex') center/cover no-repeat;
    z-index: -1;
    filter: brightness(0.55);
  }

  /* Center container */
  .center-wrap {
    height: 100vh;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 20px;
  }

  /* Main image */
  .main-img {
    width: clamp(200px, 40vw, 480px);
    max-width: 480px;
    border-radius: 30px;
    box-shadow:
      0 25px 55px rgba(0,0,0,0.85),
      0 12px 30px rgba(0,0,0,0.65),
      0 0 40px rgba(0,180,255,0.35);
    transition: transform 0.35s ease, filter 0.35s ease;
    cursor: pointer;
  }

  .main-img:hover {
    transform: scale(1.04);
    filter: drop-shadow(0 0 35px rgba(0,180,255,0.55));
  }

  /* Title */
  .title {
    margin-top: 2rem;
    font-size: clamp(1.5rem, 4vw, 3rem);
    font-weight: 600;
    text-shadow: 0 0 20px rgba(0,180,255,0.45);
  }

  /* Subtitle */
  .subtitle {
    margin-top: 0.75rem;
    font-size: clamp(1rem, 2vw, 1.35rem);
    opacity: 0.85;
  }
</style>
</head>

<body>

<div class="bg"></div>


<script>
(async () => {
  vaultBtn.style.display = "none";
  rewardsBtn.style.display = "none";
  scannerBtn.style.display = "none";
  ghostBtn.style.display = "none";
  assetBtn.style.display = "none";
  inventoryBtn.style.display = "none";
  engineBtn.style.display = "none";
  emailBtn.style.display = "none";
  videoBtn.style.display = "none";
  glassBtn.style.display = "none";
  adminBtn.style.display = "none";
  pulsemeshBtn.style.display = "none";
  pulsepalBtn.style.display = "none";
  evoToggleBtn.style.display = "none";
  driftBtn.style.display = "none";
  const badge2 = document.getElementById("pulsepal-badge");
  const talk2 = document.getElementById("pulsepal-talk");

  function pulsepalSpeak(message) {
    talk2.textContent = message;
    badge2.classList.add("talking");
    setTimeout(() => badge2.classList.remove("talking"), 10000);
  }

  pulsepalSpeak(
    "Binary OS is your dual‑state kernel — the layer where every decision collapses into 0 or 1. It governs logic flow, branching, execution certainty, and the deterministic backbone of PulseWorld. When BinaryOS.net activates, you'll be able to see your system’s raw decision architecture in its purest form."
  );
  PulseRealm.__LOAD_END = performance.now();
  console.log(`✨ PULSE MULTIVERSAL RENDERER v32.0 — %c[PULSEWORLD::EXPRESSION] Pulse World Route DUAL DOM/Network Synchronization Complete → ${(PulseRealm.__LOAD_END - PulseRealm.__DOM_START).toFixed(1)}ms`,"color:#H02FHA; font-weight:bold; font-family:monospace;");

})();
</script>

</body>
</html>
